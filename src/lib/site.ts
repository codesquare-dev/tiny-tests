// Canonical site URL used in absolute links (OG image URLs, sitemap,
// robots.txt) — next/link handles relative paths, but raw URL strings don't.
export const SITE_URL = "https://outof100.pages.dev";
